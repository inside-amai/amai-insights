# Hero and Section Two Material Refinement

## Scope
Refine only the `/homepagecopy` hero boundary and section two. Preserve all current content, typography, layout, artwork, hero image and animation, and later sections.

## Changes
- Unify all six operator rows with brass numbers, warm off-white names, warm-grey descriptions, identical spacing, weights, and single separators.
- Match the `OPERATOR FUNCTIONS` label to the brass styling of `AGENT-OPERATED POOLS.`
- Change only the scroll indicator outline and moving dot to brass while keeping its text neutral.
- Add a localized 140px static fade at the bottom of the hero into the section’s `#111312` base without changing the animated image beneath it.
- Replace section two’s flat fill with a token-based dark graphite surface, broad feathered illumination over the left side, and subtle fixed-scale monochrome grain.
- Keep rows transparent and preserve the pool SVG and denied-permissions treatment.

## Verification
- Check desktop and mobile layouts for a smooth boundary, visible material lighting, consistent rows, readable text, and unchanged neighboring sections.
- Confirm the preview builds without errors.
